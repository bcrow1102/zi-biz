(function () {
    'use strict';

    /*
      Supabase 연결은 아직 붙이지 않는다.
  
      나중에 여기서 관리할 것:
      - 카카오 간편로그인
      - users 테이블
      - gold_transactions 테이블
      - creations 테이블
      - storage 업로드
    */

    window.ZIMO_SUPABASE_READY = false;
})();